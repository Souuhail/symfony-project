{% extends 'base.html.twig' %}

{% block title %}Travail{% endblock %}

{% block body %}
    <h1>Travail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ travail.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ travail.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ travail.description }}</td>
            </tr>
           
        </tbody>
    </table>

    <a href="{{ path('travail_index') }}">back to list</a>

    <a href="{{ path('travail_edit', {'id': travail.id}) }}">edit</a>

    {{ include('travail/_delete_form.html.twig') }}
{% endblock %}
